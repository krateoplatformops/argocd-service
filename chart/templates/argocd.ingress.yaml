apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd-ingress
  labels:
  annotations:
    kubernetes.io/ingress.class: 'kong'
spec:
  rules:
    - host: {{ .Values.argocd.host }}
      http:
        paths:
          - path: /*
            pathType: Prefix
            backend:
              service:
                name: argocd-svc
                port:
                  number: 80
